apiVersion: v1
kind: Service
metadata:
  name: nginx-demo-svc
  labels:
    app: nginx-demo
spec:
  type: NodePort
  selector:
    app: nginx-demo
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 31258
